<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <property>
    <name>javax.jdo.option.ConnectionURL</name>
    <value>jdbc:postgresql://hive-metastore-db:5432/metastore</value>
  </property>
  <property>
    <name>javax.jdo.option.ConnectionDriverName</name>
    <value>org.postgresql.Driver</value>
  </property>
  <property>
    <name>javax.jdo.option.ConnectionUserName</name>
    <value>hive</value>
  </property>
  <property>
    <name>javax.jdo.option.ConnectionPassword</name>
    <value>hive</value>
  </property>

  <!-- Warehouse lives on HDFS, using the same HA nameservice as everything else -->
  <property>
    <name>hive.metastore.warehouse.dir</name>
    <value>hdfs://hdfs-ha/user/hive/warehouse</value>
  </property>

  <!-- Both HiveServer2 and Spark point here so table metadata is shared -->
  <property>
    <name>hive.metastore.uris</name>
    <value>thrift://hive-metastore:9083</value>
  </property>

  <!-- Execute queries as MapReduce jobs on YARN. No Tez in this cluster yet,
       so this stays on the classic (slower, deprecated-but-functional) MR
       engine. Swap to Tez later if you want a real speed upgrade. -->
  <property>
    <name>hive.execution.engine</name>
    <value>mr</value>
  </property>

  <property>
    <name>hive.server2.thrift.bind.host</name>
    <value>0.0.0.0</value>
  </property>
  <property>
    <name>hive.server2.thrift.port</name>
    <value>10000</value>
  </property>

  <!-- Let Hive auto-create the warehouse dir and manage stats without
       manual HDFS permission fiddling — fine for a case study, not for prod -->
  <property>
    <name>hive.metastore.schema.verification</name>
    <value>false</value>
  </property>
  <property>
    <name>datanucleus.autoCreateSchema</name>
    <value>true</value>
  </property>
  <property>
    <name>datanucleus.schema.autoCreateTables</name>
    <value>true</value>
  </property>
</configuration>
